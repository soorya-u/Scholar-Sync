/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "*.soorya-u.dev",
      },
    ],
  },
  output: 'standalone'
  
};

export default nextConfig;
