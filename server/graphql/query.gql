input LoginData {
  email: String!
  password: String!
}

input GetNexusData {
  core: String!
}

type Query {
  loginUser(input: LoginData!): String!
  logOut: Boolean!
  isUserLoggedIn: Boolean!

  getUser: Profile!
  getUserData: [Core]!
}
